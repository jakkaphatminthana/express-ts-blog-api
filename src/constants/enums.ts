export enum USER_ROLE {
  Admin = 'admin',
  User = 'user',
}

export enum BLOG_STATUS {
  DRAFT = 'draft',
  PUBLISHED = 'published',
}

export enum ORE_TYPE {
  GOLD = 'XAU',
  SILVER = 'XAG',
  PLATINUM = 'XPT',
}

export enum CURRENCY {
  THB = 'THB',
  USD = 'USD',
}
